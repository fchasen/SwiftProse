{
  "name": "lists/bullet-list-becomes-ordered",
  "tags": [
    "lists"
  ],
  "intent": "Converting a whole bullet list to an ordered list writes the right markdown, each item carries its own number, and the result survives a ProseMirror JSON round trip. The caret ends on the second item, so its spec is index 2.",
  "doc": "- one\n- two\n",
  "ops": [
    {
      "op": "selectAll"
    },
    {
      "op": "action",
      "id": "orderedList"
    }
  ],
  "expect": {
    "doc": "1. one\n2. two\n",
    "blockKind": "orderedListItem(index: 2)"
  },
  "finally": [
    "tier0",
    "projection",
    "markdownFixpoint",
    "pmJSON"
  ]
}
